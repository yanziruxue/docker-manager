# 上传说明（仅上传 Release asset，不重新构建）

适用场景：已经打好 `build-upload/docker-manager-yanzi-linux-x64.zip`（前端构建 → bundle → SEA 注入 → make-package.py 已完成），只想把它推到 GitHub Releases。

> 本文件只负责「上传」，不构建。完整一键构建+上传见 `scripts/publish.sh` 与 `docs/发布与OTA升级指南.md`。

## 方式 A：CMD（Windows 命令提示符，推荐）

在项目根目录 `D:\AI\WorkBuddy\docker-unraid` 执行：

```
set GITHUB_TOKEN=ghp_你的token
npm run release
```

（`set` 与 `npm` 分两行；变量仅对当前 CMD 窗口有效）

## 方式 B：Git Bash

```
cd /d/AI/WorkBuddy/docker-unraid
GITHUB_TOKEN=ghp_你的token npm run release
```

## 方式 C：PowerShell

```
$env:GITHUB_TOKEN="ghp_你的token"
npm run release
```

---

## 等价手动 gh 命令（备查）

```bash
export GITHUB_TOKEN=ghp_你的token
GH="/c/Program Files/GitHub CLI/gh.exe"

# Release 已存在 → 覆盖同名 asset
"$GH" release upload v1.2.1 \
  build-upload/docker-manager-yanzi-linux-x64.zip \
  -R yanziruxue/docker-manager --clobber

# 新版本首次发 → 创建 Release 并带 asset
"$GH" release create v1.3.0 \
  build-upload/docker-manager-yanzi-linux-x64.zip \
  -R yanziruxue/docker-manager -t "v1.3.0" --notes-file docs/CHANGELOG.md
```

## 注意

- Tag 必须带 `v`（如 `v1.2.1`），与 `package.json` 版本号一致，否则应用内 `compareVersion` 比对不上。
- Release 勿勾 **Draft / Pre-release**，否则应用 `releases/latest` 取不到。
- Token 仅来自环境变量，不写进本文件、不写进代码。
- 私有仓库用 `GITHUB_TOKEN` 环境变量鉴权即可，无需 `gh auth login`（后者强制要 `read:org` scope）。
- 若以后 `git init` 将整个项目推到 GitHub，务必把 `.env` 加进 `.gitignore`，勿提交 Token。
