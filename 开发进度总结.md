# Docker Stack Manager — 开发进度总结

> 更新时间：2026-08-12  
> 项目路径：`D:\AI\WorkBuddy\docker-unraid`

---

## 一、项目概述

Docker Stack Manager 是一个多引擎 Docker 管理面板，支持 Socket/TCP/SSH 三种连接方式，提供容器、镜像、数据卷、堆栈（Compose）的全生命周期管理。前端 React + Tailwind，后端 Node.js + Express + Dockerode，支持 Linux SEA 二进制部署。

---

## 二、模块完成状态

| 模块 | 状态 | 功能说明 |
|------|:----:|----------|
| 多引擎管理 | ✅ | Socket/TCP/SSH 三种连接，CRUD + 连接测试 + 持久化 |
| 仪表盘 | ✅ | 统计卡片 + 资源监控 + 活动时间线 |
| 容器管理 | ✅ | 列表/详情弹窗/操作/日志/资源监控/Web终端/CSV导出 |
| 堆栈管理 | ✅ | Compose 自动发现/创建/编辑/操作/更新检查/备份恢复/批量操作/终端 |
| 镜像管理 | ✅ | 列表/筛选/删除/prune 悬空镜像/关联容器查看 |
| 数据卷管理 | ✅ | 列表/新建/删除/prune/详情弹窗 |
| 通知中心 | ✅ | 未读/已读标记/localStorage 持久化 |
| 系统设置 | ✅ | Docker 配置/Compose 模式/通知/备份/更新调度/列可见性 |
| Web 终端 | ✅ | xterm.js + WebSocket + 多 Shell 检测 |
| Linux 部署 | ✅ | Node.js SEA 二进制 + systemd 服务 |
| Docker 部署 | ✅ | 多阶段 Dockerfile 构建 |
| **操作日志系统** | 🔨 **进行中** | 后端 logger 已完成，前端 UI 和 docker.ts 接入未完成 |

---

## 三、已修复的 Bug 清单（8/9 会话）

| # | 问题 | 根因 | 修复方式 | 涉及文件 |
|---|------|------|----------|----------|
| 1 | compose 文件名标签后两个始终高亮 | `endsWith` 匹配子串 | 提取 basename 后精确 `===` 匹配 | `Stacks.tsx` |
| 2 | 幽灵容器（thirsty_ritchie 等） | 未检查 `service` 标签 | 筛选要求 `project` + `service` 标签同时存在 | `docker.ts` |
| 3 | 编辑保存后内容消失 | transforms.ts 硬编码空值 | 从服务器返回数据 `s` 中取值 | `transforms.ts` |
| 4 | `spawnSync docker ENOENT` | Windows 未设 `shell: true` | spawnSync 选项增加 `shell: true` | `docker.ts` |
| 5 | 堆栈名称保存后不生效 | 只写 meta，不重命名目录 | `fs.renameSync` 重命名本地目录 | `docker.ts` |
| 6 | 图标 URL 不显示 | `iconUrl` vs `icon` 字段不一致 | 保存时同步写入两个字段 | `docker.ts` |
| 7 | SEA 二进制 ESM 语法错误 | esbuild ESM 格式不兼容 SEA | 改为 CJS + `BUILD_BINARY` define | `build-binary.mjs` |
| 8 | SEA 中 `__filename` 未定义 | SEA 不支持 `__filename` | banner 注入 `__SEA_URL__`，用 `process.execPath` | `build-binary.mjs` + `index.ts` |
| 9 | 堆栈"已存在"但列表不显示 | 空目录被 createStack 拒绝但被 getStacks 跳过 | 有 `.stack-meta.json` 也纳入显示；空目录允许重建 | `docker.ts` |
| 10 | Docker socket EACCES | systemd 未声明附属组 | `SupplementaryGroups=docker` + `daemon-reexec` | `docker-stack-manager.service` + `install.sh` |
| 11 | `docker compose` 不可用 | 硬编码插件模式 | `getComposeCmd()` 自动检测 + 系统设置手动选择 | `docker.ts` + `Settings.tsx` |
| 12 | 清理悬空镜像返回 HTML | 嵌入式中间件未过滤 POST/api | 仅处理 GET/HEAD，`/api/` 路径放行 | `serve-embedded.ts` |

---

## 四、当前未完成任务

### #102 — 操作日志系统 + 日志级别设置（约 60% 完成）

**目标**：所有 Docker 操作输出结构化日志到 stdout/stderr，通过 `journalctl -u docker-stack-manager -f` 查看。系统设置中可调整日志级别（debug/info/warn/error）。

#### 已完成

| 文件 | 改动内容 |
|------|----------|
| `server/logger.ts` | 新建完整的 logger 模块：`createLogger(tag)` 工厂函数、`setLogLevel()` / `getLogLevel()` 动态级别控制、4 级日志（debug/info/warn/error） |
| `server/settings.ts` | 默认设置增加 `logLevel: "info"` |
| `server/index.ts` | 启动时从设置加载日志级别；保存设置时动态更新；新增 `GET /api/settings/log-level` 端点；容器/镜像/卷操作端点已加入 `apiLog` 日志 |
| `src/types.ts` | `DockerConfig` 接口新增 `logLevel` 和 `composeMode` 字段 |
| `src/mockData.ts` | 补充 `logLevel: "info"` 和 `composeMode: "auto"` |
| `src/pages/Settings.tsx` | 初始状态包含 `logLevel: "info"` |

#### 未完成

| 待办 | 说明 |
|------|------|
| **Settings.tsx 日志级别 UI** | 在 Docker 全局配置卡片中添加日志级别 Select 组件（debug/info/warn/error），附带 journalctl 查看方法说明 |
| **docker.ts 引入 logger** | 在 `getContainers`、`getImages`、`getStacks`、`stackAction`、`removeContainer`、`removeImage`、`pruneImages`、`createVolume`、`removeVolume` 等关键函数中用 `createLogger("Docker")` 替换 `console.log` |
| **类型检查 + 构建** | `npx tsc --noEmit` 验证 → `npm run build:binary` 重新构建 bundle.js |
| **部署测试** | 复制到 Linux → `bash build.sh` → `sudo bash install.sh` → `journalctl -u docker-stack-manager -f` 验证日志输出 |

### 可清理的遗留任务

| 任务 ID | 状态 | 说明 |
|---------|------|------|
| #6 | in_progress | 早期 types.ts 重构，功能已实现，可标记完成 |
| #7 | pending | 早期 mockData.ts，功能已实现，可标记完成 |
| #8 | pending | 早期 Settings.tsx，功能已实现，可标记完成 |
| #81 | in_progress | 堆栈图标本地图片上传，未完成 |
| #82-#86 | — | deploy 旧任务，已被 #87-#94 替代 |

---

## 五、关键文件地图

### 前端（`src/`）

| 文件 | 职责 |
|------|------|
| `App.tsx` | 主入口，数据加载 + 全局状态（activeEngineId, 通知, 列可见性） |
| `pages/Containers.tsx` | 容器管理（最复杂页面，含 ContainerDetailModal） |
| `pages/Stacks.tsx` | 堆栈管理（创建/编辑/操作/备份/批量） |
| `pages/Images.tsx` | 镜像管理 |
| `pages/Volumes.tsx` | 数据卷管理 |
| `pages/Dashboard.tsx` | 仪表盘 |
| `pages/Notifications.tsx` | 通知中心 |
| `pages/Settings.tsx` | 系统设置（引擎管理 + Docker配置 + Compose模式 + 列显隐） |
| `components/UI.tsx` | 通用组件（ActionDropdown, ProgressBar, Toggle, EmptyState） |
| `components/Modal.tsx` | Modal + ConfirmDialog |
| `components/XTermTerminal.tsx` | xterm.js Web 终端 |
| `api.ts` | 所有 API 请求函数 |
| `types.ts` | TypeScript 类型定义 |
| `transforms.ts` | Docker 原始数据 → 前端模型转换 |

### 后端（`server/`）

| 文件 | 职责 |
|------|------|
| `index.ts` | Express 服务 + 所有 API 端点 + WebSocket 终端 |
| `docker.ts` | Docker API 封装（容器/镜像/卷/堆栈/stats/日志/终端/compose检测） |
| `engines.ts` | 引擎配置管理（CRUD + 持久化 + 连接检测） |
| `settings.ts` | 系统设置持久化 |
| `logger.ts` | 操作日志模块（createLogger + 动态级别） ⭐ 新增 |
| `paths.ts` | 数据目录管理 |
| `serve-embedded.ts` | 嵌入式静态文件服务（仅 GET/HEAD，API 放行） |

### 部署（`deploy/`）

| 文件 | 职责 |
|------|------|
| `docker/Dockerfile` | 多阶段 Docker 构建 |
| `linux/build-bundle.cmd` | Windows 开发机构建（vite + esbuild → bundle.js） |
| `linux/build.sh` | Linux 构建（bundle.js → SEA 二进制） |
| `linux/install.sh` | Linux 安装（systemd + docker 组权限 + daemon-reexec） |
| `linux/uninstall.sh` | Linux 卸载（--keep 保留数据 / --purge 全删） |
| `linux/docker-stack-manager.service` | systemd 服务定义 |
| `scripts/build-binary.mjs` | esbuild 构建（CJS + BUILD_BINARY + SEA URL） |

---

## 六、关键技术决策

### 1. SEA 二进制构建

- esbuild 输出 **CJS 格式**（ESM 不兼容 Node.js SEA）
- `define: { BUILD_BINARY: "true" }` 编译时注入标志，消除非二进制分支
- banner 注入 `const __SEA_URL__ = require("node:url").pathToFileURL(process.execPath).href;`
- `define: { "import.meta.url": "__SEA_URL__" }` 解决 SEA 中 `import.meta.url` 不可用

### 2. Compose 命令兼容

- `getComposeCmd()` 启动时检测：优先 `docker compose`（插件），回退 `docker-compose`（独立）
- 系统设置支持 3 种模式：自动识别 / 插件模式 / 独立模式
- 设置页面提供「检测可用命令」按钮，实时显示两种模式可用性

### 3. 嵌入式静态文件

- 二进制模式下前端文件嵌入 bundle.js（base64 编码）
- 中间件仅处理 `GET`/`HEAD` 请求
- `/api/` 路径直接 `next()` 放行，避免 API POST 被 SPA fallback 拦截

### 4. 堆栈目录扫描

- 本地 `DATA_DIR/stacks/` 目录扫描始终优先于容器标签
- 有 `.stack-meta.json` 但无 compose 文件的目录也显示（stopped 状态）
- `createStack` 只在真正存在 compose 文件时报"已存在"

### 5. Docker Socket 权限

- systemd service 声明 `SupplementaryGroups=docker`
- `ReadWritePaths` 同时声明 `/run/docker.sock` 和 `/var/run/docker.sock`
- `install.sh` 在 `usermod -aG docker` 后执行 `systemctl daemon-reexec` 刷新用户缓存

---

## 七、开发与部署流程

### 开发环境

```bash
# 开发模式（前后端同时启动）
npm run dev:all
# 前端: http://localhost:8088 (Vite)
# 后端: http://localhost:5024 (Express)

# 完整构建（前端 + 后端 TS 编译）
npm run build

# 仅后端类型检查
npx tsc -p server/tsconfig.json --noEmit

# 前端类型检查
npx tsc --noEmit
```

### Linux 部署

```
┌─────────────────────────────────────────────────────┐
│  Windows 开发机                                       │
│                                                       │
│  npm run build:binary                                 │
│  或双击 deploy/linux/build-bundle.cmd                 │
│  → 生成 deploy/linux/bundle.js (约 4.1 MB)            │
│                                                       │
│  复制 deploy/linux/ 整个目录 → Linux 服务器            │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  Linux 服务器                                         │
│                                                       │
│  cd deploy/linux/                                     │
│  bash build.sh            # 构建 SEA 二进制            │
│  sudo bash install.sh     # 安装到 /opt + 启动服务     │
│                                                       │
│  访问: http://<IP>:5024                               │
│                                                       │
│  # 查看/管理服务                                       │
│  systemctl status docker-stack-manager                │
│  systemctl restart docker-stack-manager               │
│  journalctl -u docker-stack-manager -f   # 实时日志    │
│                                                       │
│  # 卸载                                               │
│  sudo bash uninstall.sh           # 保留数据           │
│  sudo bash uninstall.sh --purge   # 全部删除           │
└─────────────────────────────────────────────────────┘
```

### NPM Scripts

| 命令 | 说明 |
|------|------|
| `npm run dev:all` | 开发模式（前后端同时） |
| `npm run build` | 完整构建（前端 + 后端 TS） |
| `npm run build:binary` | 构建二进制 bundle.js |
| `npm run build:linux` | 调用 Linux build.sh（在 Linux 上执行） |
| `npm run build:bundle` | 调用 Windows build-bundle.cmd |

---

## 八、下次继续的步骤

1. **完成日志系统 UI** — 在 `Settings.tsx` Docker 全局配置卡片中添加日志级别 Select（debug/info/warn/error），附带 journalctl 使用说明
2. **docker.ts 接入 logger** — 引入 `createLogger("Docker")`，在所有关键操作中替换 `console.log`
3. **类型检查 + 构建** — `npx tsc --noEmit` → `npm run build:binary`
4. **部署测试** — 复制到 Linux → `bash build.sh` → `sudo bash install.sh` → `journalctl -u docker-stack-manager -f` 验证
5. **清理遗留任务** — 标记 #6/#7/#8 为 completed，删除 #82-#86
